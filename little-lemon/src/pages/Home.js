import Layout from "../templates/Layout";

function Home () {
    return (<>
        <Layout>
            Home Page is not implemented because the final project did not specify implementing this page as a requirement, but the reservation page is implemented. Click on the reservation button.
        </Layout>
    </>);
}

export default Home;